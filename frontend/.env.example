# Frontend environment variables
# --------------------------------
# Leave empty in development (Vite proxy handles /api/* requests to localhost:3001).
# Set to your deployed backend URL in production (Vercel environment variable).
VITE_API_URL=
